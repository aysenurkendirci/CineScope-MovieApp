import Foundation
import UIKit

final class LoginViewModel {
    
    let rows: [LoginRow] = [
        .welcome,
        .email,
        .password,
        .loginButton,
        .googleButton,
        .footer
    ]
    
}
