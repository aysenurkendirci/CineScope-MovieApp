/*import UIKit
import SnapKit

protocol PasswordCellViewProtocol {
    var placeholder: String? { get set }
}

public class PasswordCellViewModel: PasswordCellViewProtocol {
    var placeholder: String?
    init(placeholder: String? = "Password") {
        self.placeholder = placeholder
    }
}

final class PasswordCell: BaseInputCell {
    func populate(viewModel: PasswordCellViewProtocol) {
        configure(systemIcon: "lock", placeholder: viewModel.placeholder ?? "Password", isSecure: true)
    }
}
 */
