/*import UIKit
import SnapKit

protocol EmailCellViewProtocol {
    var placeholder: String? { get set }
}

public class EmailCellViewModel: EmailCellViewProtocol {
    var placeholder: String?
    init(placeholder: String? = nil) {
        self.placeholder = placeholder
    }
}

final class EmailCell: BaseInputCell {
    func populate(viewModel: EmailCellViewProtocol) {
        configure(systemIcon: "envelope", placeholder: viewModel.placeholder ?? "Email", isSecure: false)
    }
}
 */
